## v2.6.2

三个实际使用里的老大难，一次修完。

### 📲 App 内更新，不再跳浏览器

「检查更新」发现新版后，**直接在 App 内下载 APK（带进度条）并拉起系统安装确认**——不用再切浏览器、下完再找文件了。

- 首次使用会跳系统「安装未知应用」页，允许后回到 App 自动续跑
- 下载失败自动保留「浏览器下载」兜底

### 📄 报告打不开（no entry）修复

聊天里 AI 发的报告链接有时只写文件名（如 `BIW_RI_报告.md`），而文件实际在子目录（`BIW_RI\BIW_RI_报告.md`），旧版按根目录找直接「读取失败」。现在打开失败时会**自动在工作区内递归搜索同名文件**（深度 4 层，跳过 node_modules/.git 等），找到即定位打开，并提示实际路径。

已用真实案例验证：BIW 会话的 `BIW_RI_报告.md` 现在能自动定位到 `D:\Project\Simulation\CST\BIW_RI\` 下并完整渲染（含图片）。

### 📷 扫码修复

- 修复相机预览画面被**拉伸变形**（就是「二维码变窄」的真相）：预览改为按相机比例等比缩放居中
- 解码增加**中央 2/3 区域优先**裁剪，屏幕上的码相对更大，识别更快更稳；全帧解码保留兜底

---

**下载**：见下方 Assets，或 CDN 直链 `https://cdn.jsdelivr.net/gh/teslawei/dsh-remote-dist@main/apk/DSH-Remote-Direct-v2.6.2.apk`（也可在旧版 App 内「ⓘ 检查更新」直接升级）。

覆盖安装即可，配对数据保留。

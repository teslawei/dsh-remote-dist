## v2.6.3 — 扫码真机修复

v2.6.2 修好了预览变形，但真机上还是**一直扫不出码**——这版专治这个。

### 📷 扫码解不出的真机修复

从你的截图看预览已经正常了（比例对、码清晰），但解码一直在失败。真机和模拟器扫「屏幕上的码」有三个经典差异，这版一次处理：

- **预览格式**：之前没显式指定，部分机型默认给 YV12 而不是 NV21——解码按 NV21 读数据直接全错。现在显式请求 NV21。
- **二值化策略**：ZXing 默认的 Hybrid 算法是为拍照场景设计的，对**发光屏幕**上的码（亮度渐变、反光、摩尔纹）经常失败；Global 全局直方图算法在屏幕码场景反而更稳。现在两个都试。
- **解码梯队**：每帧按「中央裁剪 / 全帧 × 全局 / 混合 × 旋转 / 反相」六路依次尝试，反相那路专门应对深色主题屏幕、反光导致的黑白反转。
- 另外加了诊断日志（`adb logcat -s ScanActivity`），以后再扫不出可以直接看解码过程。

### 📲 升级方式

旧版 App（v2.6.1+）直接 **ⓘ → 检查更新 → 下载并安装**，App 内完成，不用再跳浏览器。

CDN 直链：https://cdn.jsdelivr.net/gh/teslawei/dsh-remote-dist@main/apk/DSH-Remote-Direct-v2.6.3.apk

---

若这版还扫不出，请告诉我手机型号，或跑一次 `adb logcat -s ScanActivity` 把几行日志发我——每 32 帧会打一条解码记录，能直接定位是哪一环。

# v2.6.1 报告图片显示

## 修复
- **报告图片全部破图的根因修复**：图片读取接口的偏移量按 1 起计，而服务器从 0 起计——每张图都丢失第一个字节，PNG/JPEG 文件头残缺无法解码；修复后报告内图片完整显示
- **图片加载方式升级**：图片改为按 2MB 分块读取、渲染前以 data URI 内嵌进页面，打开即完整呈现，不再依赖二次子资源请求（大图加载稍慢属正常）
- **md 报告打不开修复**：反引号代码段里的文件路径现在也能识别为可点链接
- **md 显示 base64 原文修复**：部分接口返回的 base64 因解码符号处理错误被原样显示，现已正确解码为中文内容
- 报告内的相对链接点击不再误开浏览器，统一在 App 内跳转

## 新功能
- **会话归档**：会话列表长按任意会话 → 「归档（从列表移除）」，不想要的会话可以从列表清掉（服务器端不物理删除，需要时可恢复）

## 安装
覆盖安装即可。

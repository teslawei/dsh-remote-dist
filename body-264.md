## v2.6.4 — 扫码：实时诊断 + 相册兜底

真机上还是扫不出没关系，这版让你**看得见问题在哪**，并且给了一条**几乎必成的兜底路径**。

### 🔍 扫码页实时状态
- 取景框下方新增状态行：**「已分析 N 帧，未识别」**——数字在涨说明相机帧正常进来、解码一直在跑（是码难识别）；数字不动说明帧没进来（是相机回调问题）。一眼分清是哪种。
- **点一下状态行**，完整诊断信息（App 版本、手机型号、Android 版本、相机参数、已分析帧数、异常记录）复制到剪贴板——**粘贴发给开发者即可**，不需要开发者模式、不需要连电脑。

### 🖼 从相册选图识别（兜底，推荐先试这个）
预览流扫码受对焦、反光、帧分辨率限制，扫屏幕上的码天然困难。稳妥做法：
1. 先用手机**系统相机**对着电脑屏幕**拍一张配对二维码的照片**（照片是全分辨率，比取景流清晰得多）
2. 打开 App 的扫码页 → 点**「从相册选图识别」** → 选中刚拍的照片
3. App 对照片做全分辨率识别（同样支持反相/多算法），成功即自动填入

### 📲 升级方式
旧版 App **ⓘ → 检查更新 → 下载并安装**。

CDN 直链：https://cdn.jsdelivr.net/gh/teslawei/dsh-remote-dist@main/apk/DSH-Remote-Direct-v2.6.4.apk

---

**反馈方式**：升级后扫一次码（对准二维码停 5 秒）→ 点状态行复制诊断 → 粘贴发给开发者。如果帧数一直为 0 或涨但不识别，附一句当时的场景（距离/角度/光照）即可。

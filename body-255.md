# v2.5.5 更新通道修复

## 修复
- **检查更新失败**：版本查询与 APK 下载切换到公开分发仓库（jsDelivr 全球 CDN 兜底），不再依赖原开发仓库的可达性
- 检查更新扩展为 4 条通道（GitHub API → 跳转解析 → jsDelivr API → jsDelivr CDN 的 VERSION 文件）
- 失败时显示具体诊断信息（卡在哪个端点），超时放宽到 12 秒
- 「下载 APK」默认走 CDN 直链，「发布页」按钮指向 GitHub Releases

## 延续 v2.5.3
- 权限审批卡片（一键批准/拒绝/稍后再说）与重复弹窗修复

**安装**：下载 APK 直接覆盖安装（需已完成配对）。

# v2.6.0 报告预览

## 新功能
- **聊天内报告预览**：对话里提到的项目报告文件名（md / html / txt / log / json / csv / 图片）自动变成可点链接
- 点开即在 App 内渲染：Markdown 本地转 HTML 排版（标题/列表/表格/代码块/引用），HTML 原样呈现（含内嵌 SVG 图表与 base64 图片）
- 报告内的相对链接可继续点击跳转；图片子资源自动从工作区拉取
- 数据经远程通道按会话工作区隔离读取（workspaceFiles/readAll / readBytes），越界路径服务器拒绝
- 非 HTML 的 base64 返回自动解码；PDF 检测后提示暂不支持手机预览

## 实测
BIW 车身仿真 4.1 MB 客户报告（含 7 张 SVG 图表）在真机分辨率下完整渲染。

**安装**：覆盖安装即可。
